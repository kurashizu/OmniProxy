[workspace]
members = ["protocol", "client", "server", "proxy"]
resolver = "2"

[workspace.package]
version = "1.0.2-beta.2"
edition = "2021"

[profile.release]
opt-level    = "z"
strip        = true
lto          = true
codegen-units = 1
panic        = "abort"
