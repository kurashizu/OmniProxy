[workspace]
members = ["protocol", "client", "server", "proxy", "gui"]
resolver = "2"

[workspace.package]
version = "1.0.3-rc.1"
edition = "2024"

[profile.release]
opt-level    = "z"
strip        = true
lto          = true
codegen-units = 1
panic        = "abort"
