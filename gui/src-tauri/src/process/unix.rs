// Unix-specific helpers. Currently empty; `process/mod.rs` handles SIGTERM
// inline because the only difference between Unix and Windows is whether
// SIGTERM should be tried first. Kept as a stub so the file structure
// matches the plan and can be extended for future Unix-specific needs.
